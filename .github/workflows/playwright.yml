name: Playwright Tests

on:
  push:
    branches-ignore:
      - main
  pull_request:
    branches:
      - main

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Skip Playwright tests
        run: echo "Playwright GitHub Actions workflow is disabled; tests are not run in CI."